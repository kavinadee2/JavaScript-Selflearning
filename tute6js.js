let length;
let width;
let result;
length = window.prompt(`Enter a length of rectangle:`);
width = window.prompt(`Enter a width of rectangle:`);
result = (1/2)*length*width;
document.getElementById("myp1").textContent=`result= ${result}`;

